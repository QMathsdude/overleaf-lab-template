% -----------------
% 1. Define Content Design

% Define style
\fancypagestyle{reportContent}{

    % Clear all header and footer fields
    \fancyhf{} 

    % Header
    \fancyhead[R]{\tblue{\fontfamily{phv}\selectfont\textbf{\nouppercase{\leftmark} : \reporttitle}}}
    \fancyhead[L]{\includegraphics[height=15pt]{Figures/Logo/UM.png}}
    % \renewcommand{\headrulewidth}{0pt}
    % \renewcommand{\headrule}{{\color{UM-blue} \hrule height 1pt}}
    \renewcommand{\headrule}{\vspace{-10pt}\color{UM-blue}\makebox[\textwidth][r]{\rule{0.5\textwidth}{0.5pt}}}
    % \fancyhead[L]{\tblue{\textbf{\reporttitle}}}
    
    % Footer
    \fancyfoot[R]{\tblue{\small\fontfamily{phv}\selectfont \textbf{Page \thepage}}}
    % \renewcommand{\footrulewidth}{0.4pt}
    % \rfoot{Page \thepage}
}

% -----------------
% 2. Define Minimalist Design

% Define style
\fancypagestyle{reportMinimalist}{

    % Clear all header and footer fields
    \fancyhf{} 

    % Header
    \fancyhead[L]{\includegraphics[height=15pt]{Figures/Logo/UM.png}}
    % \renewcommand{\headrulewidth}{0pt}
    % \renewcommand{\headrule}{{\color{UM-blue} \hrule height 1pt}}
    \renewcommand{\headrule}{\vspace{-10pt}\color{UM-blue}\makebox[\textwidth][r]{\rule{0\textwidth}{0pt}}}
    % \fancyhead[L]{\tblue{\textbf{\reporttitle}}}
    
    % Footer
    \fancyfoot[R]{\tblue{\small\fontfamily{phv}\selectfont \textbf{Page \thepage}}}
    % \renewcommand{\footrulewidth}{0.4pt}
    % \rfoot{Page \thepage}
}